% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{d1_export}
\alias{d1_export}
\alias{d1_download}
\alias{d1_download_sqlite}
\title{Export a D1 database}
\usage{
d1_export(
  database_id,
  tables = NULL,
  no_data = FALSE,
  no_schema = FALSE,
  poll_interval = 1,
  account_id = d1_account(),
  token = d1_token()
)

d1_download(
  database_id,
  path,
  tables = NULL,
  account_id = d1_account(),
  token = d1_token()
)

d1_download_sqlite(
  database_id,
  path,
  tables = NULL,
  account_id = d1_account(),
  token = d1_token()
)
}
\arguments{
\item{database_id}{Database UUID.}

\item{tables}{Optional character vector restricting the export to these
tables.}

\item{no_data}{If \code{TRUE}, export only the schema.}

\item{no_schema}{If \code{TRUE}, export only the data.}

\item{poll_interval}{Seconds between polls while the export is prepared.}

\item{account_id, token}{Cloudflare credentials. See \code{\link[=d1_token]{d1_token()}}.}

\item{path}{Destination file path.}
}
\value{
\code{d1_export()} returns a list with \code{filename} and \code{signed_url}.
\code{d1_download()} and \code{d1_download_sqlite()} return \code{path} invisibly.
}
\description{
\code{d1_export()} triggers a SQL dump and polls until Cloudflare has staged the
file, returning the result containing a signed download URL.
\code{d1_download()} is a convenience wrapper that exports and downloads the SQL
dump to a local file. \code{d1_download_sqlite()} goes one step further,
materialising the dump into a local SQLite database that can be opened with
RSQLite, mirroring the types of the live database.
}
