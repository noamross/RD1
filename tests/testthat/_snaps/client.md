# missing credentials raise an informative error

    Code
      d1_token()
    Condition
      Error in `resolve_cred()`:
      ! No Cloudflare token found.
      i Pass `token`, set `options(RD1.token = )`, or the `CLOUDFLARE_API_TOKEN` environment variable.

